# Copy this file to .env and fill in values. Do NOT commit .env.

# App DB credentials (used by both compose stacks)
APP_DB_USERNAME=sparta_user
APP_DB_PASSWORD=change_me

# Local MySQL settings (docker-compose.local.yml)
MYSQL_ROOT_PASSWORD=local_root_password
MYSQL_ADMIN_PASSWORD=local_admin_password

# Optional override for remote DB URL (docker-compose.remote.yml)
# DB_URL=jdbc:mysql://204.197.161.186:3306/sparta_academy
